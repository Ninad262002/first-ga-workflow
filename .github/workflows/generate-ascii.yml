name: generate ascii artwork
on: push
jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v6
            - name: list all files
              run: ls -ltra
            - name: Executing shell script
              run: ./ascii-script.sh

